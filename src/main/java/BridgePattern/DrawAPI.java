package BridgePattern;

public interface DrawAPI {
    public void drawCircle(int radius,int x, int y);
    // radius -> its the radius of circle
    // int x and int y -> are the coordinates of the circle. (x,y) arbitary point.
}
